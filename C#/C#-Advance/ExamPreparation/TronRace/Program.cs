using System;
using System.Linq;
using System.Net.Http.Headers;

namespace TronRace
{
    class Program
    {
        static void Main(string[] args)
        {
            int tottalRows = int.Parse(Console.ReadLine());
            char[][] battleField = new char[tottalRows][];

            for (int row = 0; row < tottalRows; row++)
            {
                         
            }

        }
    }
}
